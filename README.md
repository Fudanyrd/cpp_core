#I collected some commonly used data structures in C++ programming languages.

They are: Vec(vector), String, dblList(doubly linked list), set, dictionary(map) and so on.

for detailed information, see dependencies.md
